# 14 — Troubleshooting (Real World)

Common errors:
1. Permission denied → RBAC/ACL mismatch
2. Cannot list directory → missing execute permission
3. Works in portal but fails in Synapse → MI missing access
4. Private endpoint issue → DNS not configured
5. Slow serverless query → CSV + small files + no partitions
