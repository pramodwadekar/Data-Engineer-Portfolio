# Common Errors and Fixes

1. Private endpoint created but fails → DNS not linked
2. Synapse cannot access → private endpoint approval pending
3. Portal works but pipeline fails → MI/IR network mismatch
